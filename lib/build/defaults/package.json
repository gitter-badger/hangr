{
  "name": "appName",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "author": "First Last <EMAIL>",
  "dependencies": {
    "hangr": "^0.0.5"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
    "prestart": "npm prune && npm install"
  },
  "license": "ISC"
}