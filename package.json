{
  "name": "hangr",
  "description": "Isomorphic Javascript, Connected Client, Web Application Stack",
  "version": "0.0.5",
  "homepage": "http://hangr.software",
  "author": "David Clutter <cluttered.code@gmail.com>",
  "repository": {
    "type": "git",
    "url": "git://github.com/hangr/hangr"
  },
  "preferGlobal": true,
  "bin": {
    "hangr": "./bin/hangr.js"
  },
  "files": [
    "bin",
    "lib"
  ],
  "keywords": [
    "hapi",
    "angular",
    "rethinkdb",
    "framework",
    "http",
    "api",
    "web",
    "application",
    "stack",
    "isomorphic",
    "connected client"
  ],
  "dependencies": {
    "commander": "^2.9.0"
  },
  "devDependencies": {
    "code": "^1.5.0",
    "lab": "^6.2.0"
  },
  "scripts": {
    "pretest": "npm install",
    "test": "lab -a code -r console -o stdout -r html -o coverage.html -v -t 80",
    "precitest": "npm run pretest",
    "citest": "lab -c -a code -r console -o stdout -r tap -o tests.tap -r clover -o coverage.xml -v -t 80"
  },
  "bugs": {
    "url": "https://github.com/hangr/hangr/issues"
  },
  "engines": {
    "node": ">=4.0.0"
  },
  "license": "MIT"
}